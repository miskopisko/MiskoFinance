using MPersist.Core;
using MPersist.Message.Request;

namespace MPFinanceCore.Message.Requests
{
    public class TestConnectionRQ : RequestMessage
    {
        private static Logger Log = Logger.GetInstance(typeof(TestConnectionRQ));

        #region Parameters



        #endregion

        public TestConnectionRQ()
        {
        }
    }
}