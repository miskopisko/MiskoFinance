using MiskoPersist.Core;
using MiskoPersist.Message.Response;

namespace MiskoFinanceCore.Message.Responses
{
    public class TestConnectionRS : ResponseMessage
    {
        private static Logger Log = Logger.GetInstance(typeof(TestConnectionRS));

        #region Parameters



        #endregion

        public TestConnectionRS()
        {
        }
    }
}